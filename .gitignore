# Secrets -- plaintext only, never committed (no encryption-at-rest; see
# secrets/.gitkeep). Keep values out of git entirely instead.
secrets/secrets.json

# Flake lock backups
flake.lock-backup-*

# Nix build artifacts
result
result-*

# MCP config (project-level)
.mcp.json

# Claude Code local settings
.claude/settings.local.json

prompt.txt
AGENTS.md
